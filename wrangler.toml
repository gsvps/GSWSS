name = "gswss"# Cloudflare Workers 部署配置（一键部署读取此文件）
# Worker 名称不在仓库中预填，部署页由用户自行填写
main = "worker/src/index.ts"
compatibility_date = "2025-03-01"

[vars]
# WebSocket 入口路径（客户端 server 地址须包含此路径，例如 /proxy）
WEBSOCKET_PATH = "/ws"
# 认证密码（客户端 config.yaml 中的 password 须与此一致，部署前请修改）
PASSWORD = "change-me"
